<div>
	<h1>Welcome to Alexandra Doffing Design!</h1>
	<p>
		Take a look around! This is a brand new site so there will be plenty of new content and features to come but in the meantime, check out 
		my projects below!  If you like what you see or you simply want to send me sweet words via email, you can get my contact information via the Contact page.
	</p>
	<div>
		<% @project_groups.each do |g|%>
		<div class="cf">
			<h3> <%= g[:p_type] %></h3>
			<% g[:projects].each_with_index do |proj, i| %>
			<% col_idx = "col-" + ((i % 3) + 1).to_s %>
			<div class="<%= col_idx %> project">
				<% unless @isAdmin.nil? %>
				<a class="edit-tag" href="/admin/edit/<%= proj.id %>"></a>
				<% end %>
				<a href="<%= project_url(proj.id) %>"><img src="<%= proj.thumbnail_image %>" /></a>
				<a href="<%= project_url(proj.id)  %>"><%= proj.project_title %></a>
			</div>
			<% end %>
		</div>
		<% end %>
		<% unless @isAdmin.nil? %>
		<p>
			Dis is an admin block. This should show at admin/index
		</p>
		<% end %>
	</div>
</div>